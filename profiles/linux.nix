# Intentionally empty placeholder: mkHomeModules in lib/hosts.nix always
# imports a platform profile (darwin.nix or this one), so Linux-only home
# config has a ready home when it's needed.
{ ... }:

{
}
